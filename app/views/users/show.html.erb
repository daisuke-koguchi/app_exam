<div class="container">
  <div class="alert alert-info">
    　ログインに成功したら、「ブログ一覧画面へ」ボタンを押してブログと写真を投稿しよう。
  </div>
  <%= notice %>
  <table class="table">
    <tbody>
    <tr>
      <th>名前</th>
      <td><%= @user.name%></td>
    </tr>
    <tr>
      <th>メールアドレス</th>
      <td><%= @user.email%></td>
    </tr>
    <tr>
      <th>作成日時</th>
      <td><%= @user.created_at%></td>
    </tr>
    </tbody>
  </table>
  <%= link_to "ブログ一覧画面へ", blogs_path, class: 'btn btn-primary' %>
</div>
